import styled from "styled-components";

export const Wrapper = styled.div``;

export const Image = styled.img`
  width: 50px;
  position: absolute;
  bottom: 10px;
  left: 5;
  transform: scale(-1, 1);
`;
